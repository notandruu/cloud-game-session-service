apiVersion: v1
kind: Service
metadata:
  name: cloud-game-session-service
spec:
  selector:
    app: cloud-game-session-service
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
